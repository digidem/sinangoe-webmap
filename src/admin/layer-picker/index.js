import { Control } from './control'
import { Preview } from './preview'

const Widget = {
  name: 'mapbox-layer-picker',
  controlComponent: Control,
  previewComponent: Preview
}

export {
  Widget,
  Control,
  Preview
}
